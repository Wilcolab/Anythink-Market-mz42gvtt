/**
 * Write a JavaScript function toCamelCase that converts a string into camelCase.
 *
 * Example:
 * toCamelCase("hello world") should return "helloWorld"
 *
 * Requirements:
 * - Convert spaces/underscores/dashes into word boundaries.
 * - The first word should be lowercase.
 * - Each following word should start with an uppercase letter.
 * - Return the converted camelCase string.
 */
